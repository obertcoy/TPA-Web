export interface User {
    id: string;
    first_name: string;
    last_name: string;
    email: string;
    gender: string;
    dob: Date;
    profileImageURL: string;
}

export interface ShowUser{

    id: string
    first_name: string
    last_name: string
    profileImageURL: string
}